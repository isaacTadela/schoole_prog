
/**
 * This package contains entity classes that can be used by both hs-client and hs-server packages.
 * So in order to prevent code duplication by creating same Entity classes twice we had them separated as library
 * to be imported by both hs-server and hs-client.
 *
*/

package common;
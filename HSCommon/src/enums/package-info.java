
/**
 * This package contains enums that can be used by both hs-client and hs-server packages.
 * So in order to prevent code duplication by creating same Entity Enums twice we had them separated as library
 * to be imported by both hs-server and hs-client.
 *
*/

package enums;